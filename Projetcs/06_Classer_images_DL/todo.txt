voir input 7 X 7

Q :
- séparer couleur et forme ? (conv2d sur grayscale & [average -> 1x1 conv] color ?)
- Scheduler = vmin + (vmax-vmin) * (0.96**epoch) ? vmin = 0. ?

voir ce genre
-----  vmax
     .
      .
       .
         ---- vmin


transfer learning : ajout fine tining ? pas sur avec les résultats (à noter)


dropout : ajout test avec / sans

après tous les test : augmenter réseau pour over-fit puis régulariser

RANDOM SEED (data aug, dataloader, etc...)

voir possibilité de tracer l'idx de l'image
|-> regarder loss par image en fonction du batch (map)

"dropout does not seem to play nice with batch normalization."

visualisation des filtres, jusqu'à quelles couches ?
|- comment faire avec non-linéarités ?


voir augmenter AdaptativeAvgPool2d ?


compute + plot confusion matrix

test taille kernels

novelty detection :
- entrainer cnn + classification
- essayer entrainer decodeur ?


AE : train -> save -> reload au besoin

256475351


- from tempfile import TemporaryDirectory
    with TemporaryDirectory() as tempdir:
        best_model_params_path = os.path.join(tempdir, 'best_model_params.pt')





https://github.com/ageron/handson-ml2/blob/master/14_deep_computer_vision_with_cnns.ipynb

Geron Aurélien : hands on ml with scikit learn and tensorflow 2nd edition
Lire chapitres 10 à 14

voir data augmentation avec pytorch

random resize crop
random horizontal flip
random rotation

sélection du top 15 / 10 des tags (les + présentes)

voir utilsation gpu tensorflow
https://www.tensorflow.org/install/gpu?hl=fr

filtre  non-local-means


VGG16, VGG19
Inception resnet50 GPT4 ?

https://github.com/ageron/handson-ml2/blob/master/14_deep_computer_vision_with_cnns.ipynb


hyper paramètres les plus importants :
nombre couche
nombre neurones par couche
learning rate


torchvision.transforms.RandomResizedCrop(size, scale=(0.08, 1.0), ratio=(0.75, 1.3333333333333333))
torchvision.transforms.RandomResizedCrop(size, scale=(0.08, 1.0), ratio=(0.99, 1.01))

TorchScript
